{
  "name": "more-svelte-components",
  "version": "1.0.0",
  "type": "module",
  "description": "Svelte components for More MUD",
  "scripts": {
    "build": "vite build",
    "build:component": "node build-component.js",
    "build:all": "npm run build:hello-world && npm run build:counter && npm run build:3d-cube",
    "dev": "vite",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "svelte": "^4.2.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "three": "^0.158.0"
  }
}